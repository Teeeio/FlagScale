- serve_id: vllm_model # Not in use
  engine_args:
    host: 0.0.0.0
    port: 6000
    model_variant: QwenGr00t
    model: /share/project/fengyupu/github/FlagScale_2/outputs/qwen_gr00t_train_zero2_norm_gripper_update_imagepre/checkpoints/030000
    device: "cuda"
    images_keys:
      - observation.images.base_0_rgb
      - observation.images.left_wrist_0_rgb
      - observation.images.right_wrist_0_rgb
    # Only used for warmup
    images_shape: [3, 480, 640]
    state_key: observation.state
